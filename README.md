# Cartoonification_of_images
Machine Learning model that utilizes OpenCV for processing and cartoonifying any image that is added to the model
Required libraries in the system:
-opencv
-numpy
-matplotlib
-easygui
-tkinter
